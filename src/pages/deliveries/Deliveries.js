import { Fragment } from "react";
import Box from "../../components/Box";
import Typography from "../../components/Typography";

export default function DeliveriesPage() {
  return (
    <Fragment>
      <Box>
        <Typography variant="h2">Delivery Request</Typography>
      </Box>
    </Fragment>
  );
}
